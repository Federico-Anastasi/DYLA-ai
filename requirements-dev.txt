# Dependencies for developing and testing Dyla. Users don't need these.
#   pip install -r requirements-dev.txt
#   python -m pytest server/tests
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
# FastAPI's TestClient runs on it: without it the API tests can't start.
httpx>=0.27
